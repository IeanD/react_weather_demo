class BoreasHeader extends React.Component {
    render() {
        return (
            <header className="title-header">
                <h1 className="title"> {this.props.title} </h1>
            </header>
        )
    }
}
